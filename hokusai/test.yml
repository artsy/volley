---
version: '2'
services:
  volley:
    command: yarn jest
    environment:
      - NODE_ENV=test
      - DATADOG_AGENT_HOSTNAME=test-hostname
    extends:
      file: build.yml
      service: volley
